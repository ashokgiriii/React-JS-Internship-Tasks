import { Toaster } from 'react-hot-toast'

const CustomToast = () => {
    return (
        <Toaster
            position="top-center"
            reverseOrder={false}
        />
    )
}

export default CustomToast